const server = 'https://project-manager-app-staging.onrender.com'
const apiVersion = '/api/v1'

export const serverUrl = {
    projects: {
        base: server + apiVersion + '/projects/'
    }
}